---
target: src/app/page.js
total_score: 15
max_score: 32
na_heuristics: 7,10
p0_count: 2
p1_count: 2
target_identity: "file:/home/homonculus/projects/cv/cv-max/src/app/page.js"
target_fingerprint: "sha256:3ae1054ea37be5e1cdbdf876e8a8a5ca4a3b6ed100a366de0ca42e880e079b5f"
target_path: /home/homonculus/projects/cv/cv-max/src/app/page.js
timestamp: 2026-09-25T09-49-01Z
slug: src-app-page-js
---
# Critique — CV Maxime Tancrède (src/app/page.js + /Scantrad)

Method: dual-agent (A: design review · B: detector + browser evidence). Not degraded.
Browser-tooling note: claude-in-chrome unavailable this session; both agents used headless-Chromium CDP automation instead. Injection succeeded and console findings were captured, but no visible [Human] tab was shown to the user.

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|---|---|---|
| 1 | Visibility of System Status | 2/4 | No "sending…" state on contact button; slider gives no position indicator. |
| 2 | Match System / Real World | 3/4 | Bio is natural French; form success/error strings are English. |
| 3 | User Control and Freedom | 2/4 | No form reset, no back-to-top, unlabeled Scantrad back-link. |
| 4 | Consistency and Standards | 1/4 | /Scantrad runs a different palette (detector: 5 "Purple/violet neon" hits vs homepage cyan/magenta). .link buttons fill gradient, contradicting DESIGN.md's "ring never fill" rule. |
| 5 | Error Prevention | 1/4 | Zero client-side validation on contact form. |
| 6 | Recognition Rather Than Recall | 2/4 | Icon-only unlabeled social row + back-link; unrecognizable logos. |
| 7 | Flexibility and Efficiency | n/a | Static one-person CV, no expert workflow. |
| 8 | Aesthetic and Minimalist Design | 3/4 | Strong palette where followed; 58 "cyan neon on dark" detector hits on Competences grid (brand-logo caveat applies); confirmed hero overflow. |
| 9 | Error Recovery | 1/4 | Blocking native English alert() on send failure; detector confirms 1.2:1 contrast on that button. |
| 10 | Help and Documentation | n/a | No help surface needed. |
| Total | | 15/32 | Poor (47%) |

## Design Specificity Verdict

LLM: Real signature (gradient ring, void-teal, AllertaStencil/Inconsolata) applied with discipline on the homepage; specific bio/project copy. But specificity is skin-deep — no photo of Maxime (generic </> glyph repeated 6+ times), and the one case-study page (/Scantrad) abandons the system entirely.

Deterministic scan: `impeccable detect --json src/app src/component src/portfolio` -> 34 findings, all advisory (design-system-color), 0 primary, exit 0. Several live in dead code (IconHeader.css/TabBar.css, commented out in Home.js).

Browser pass (injected detect.js): 95 individual finding lines on / (tool's own summary said 81 - tool-side tally discrepancy). 75 ai-color-palette hits (58 "cyan neon on dark" on Competences grid - brand-logo caveat, detector can't distinguish logo color from authored aesthetic). 14 low-contrast hits (13x white-on-cyan 1.5:1 on a.link - same element as the DESIGN.md ring-vs-fill violation; 1x contact button 1.2:1). 1 text-overflow hit on div.top-pres-left overflowing by 30px - independently confirms Assessment A's P0 hero-name finding via layout measurement. 5 hits on /Scantrad, all "Purple/violet neon" - confirms it runs a disconnected palette. 3 unrelated React console errors: missing key in CompetenceList, class vs className in TalentCase.js, stray `t` attribute in ProjectCase.js's <span className="description" t>.

Visual overlays: no visible [Human] tab this run (see browser-tooling note).

## Overall Impression

Strong point of view for the first ~900px, lost twice: the unlabeled Competences icon wall, and completely on /Scantrad. The contact form (the actual conversion moment) is the least-polished screen. The system built is good; it isn't finished being applied.

## What's Working

1. Gradient-ring device is a genuine, consistently-executed signature on the homepage.
2. Desktop hero fold is confident and well-composed.
3. Bio and project copy are specific and personal, not templated.

## Priority Issues

[P0] Hero name clipped on mobile, detector-confirmed real layout overflow (not just viewport judgment).
Why it matters: identity-critical element unreadable on the dominant device class.
Fix: Presentation.css line 4 `.name{font-size:128px}` has no mobile override - add `font-size: clamp(40px, 13vw, 128px)` inside the existing @media(max-width:1000px) block.
Suggested command: /impeccable adapt

[P0] /Scantrad abandons the entire design system, confirmed by an unrelated color palette firing only there.
Why it matters: the one page proving depth of work reads as a different, unfinished site.
Fix: rebuild Scantrad.js/.css on PartContener + void-teal + the ring technique.
Suggested command: /impeccable polish

[P1] Contact form: no validation, no loading state, blocking English alert()s, 1.2:1 contrast failure on send button text.
Why it matters: the site's actual conversion moment is its least reassuring screen.
Fix: add required/email-pattern validation, a disabled "Envoi..." state, inline French error message instead of alert(), fix button text/background contrast.
Suggested command: /impeccable harden

[P1] Hero social icons point to generic platform homepages, not real profiles.
Why it matters: Presentation.js lines 55/57 open linkedin.com/ and github.com/ - dead ends; his real GitHub is used correctly elsewhere in the codebase.
Fix: point both to his real profile URLs.
Suggested command: /impeccable clarify

[P2] Competences is an unlabeled 18-icon wall, confirmed by low-contrast and palette findings concentrated on that grid.
Why it matters: already-categorized data (competences2.json) never surfaces visually; textbook Visual Noise Floor.
Fix: render the 4 existing categories as labeled sub-groups.
Suggested command: /impeccable layout

## Persona Red Flags

Casey (mobile): name cut off at both edges (detector-confirmed overflow); slider arrows hidden below 1000px with no dots/peek, no cue more projects exist.
Jordan (first-timer): mail icon fires a native alert() reading as a security prompt; 18 unlabeled logo tiles, several unidentifiable.
Sam (accessibility): html lang="en" on all-French content; icon-only links with no aria-label; no dedicated focus treatment on a bespoke dark theme per DESIGN.md itself.

## Minor Observations

- layout.js still ships CRA placeholder title/description.
- React console warnings: missing key in CompetenceList, class vs className in TalentCase.js, stray t attribute in ProjectCase.js.
- .link buttons fill the gradient instead of ringing it, contradicting DESIGN.md, and concentrate the 13 low-contrast hits.
- Dead code: Home.js's unused downloadButton() points to a different CV URL than the one actually used.
- Same </> hex glyph reused 6+ times standing in for a personal photo.
- Discord/Mail icon clicks confirm via blocking native alert(), inconsistent with the rest of the interaction language.
- Detector's own home-page summary under-reports its count (81 vs 95 printed lines) - tool-side issue, not a site defect.

## Questions to Consider

1. Most confidence invested in the opening 900px, least in the contact form close - why?
2. DESIGN.md says the gradient is never a fill, but it is, on every project-card button and across /Scantrad - documenting what shipped or what was intended?
3. Would one real photo of Maxime do more than a sixth </> glyph?
